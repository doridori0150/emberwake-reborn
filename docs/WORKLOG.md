# 작업 기록 — Emberwake Reborn v0.1.0

2026-09-17. 작업자: Claude(Claude Code, 데스크톱 앱 내장 브라우저로 직접 플레이). 서브에이전트는 쓰지 않았다.

## 위치와 보존

- 새 프로젝트: `D:\Project_home\Game\EmberwakeReborn` (작업 시작 시 폴더가 없어 새로 만들었다).
- 참조 프로젝트 `EmberwakePrototype`은 **읽기만** 했다. 실행용 WebP 85개를 `assets/`로 복사했고 원본 파일·저장·`dist`는 건드리지 않았다. 원본 주소 `127.0.0.1:8791`은 열지 않았다.
- 미리보기 서버 설정용으로 `D:\Project_home\.claude\launch.json`(포트 8812 항목 1개)을 새로 만들었다.

## 개선 주기 (관찰 → 원인 → 수정 → 변화 → 남은 문제)

| # | 관찰한 문제 | 원인 가설 | 수정 | 실제 변화 | 남은 문제 |
| --- | --- | --- | --- | --- | --- |
| 1 | 바닥 타일 사이에 검은 틈, 소품이 작게 보임 | 아틀라스 셀 안쪽에만 그림이 있음 | 셀별 실제 경계를 실측(PIL)해 매니페스트에 기록 | 타일이 빈틈없이 이어짐 | 벽은 한 장의 성벽 그림 반복 |
| 2 | 시작하자마자 영웅이 기둥 위에 서 있음 / 기둥 뒤가 새까맣게 보임 | 시작 칸이 보호 칸 목록에 없음 / 지형 기억까지 시선 판정 | 시작 칸 보호, 지형 기억은 반경만·적은 시선으로 | 첫 화면이 읽힘 | — |
| 3 | 어둠 속 사수가 안 보이는 채로 조준 | 미탐사 칸이 유닛을 가림 | 경계 중인 적 주변 칸을 드러냄 | 조준선의 출처가 항상 보임 | — |
| 4 | 밀어치기 미리보기가 "피해 2"만 표시(실제 5) | 충돌·지형 피해가 미리보기 계산 밖 | `shoveForecast`로 충돌·지형·덫을 예측해 합산, 도착 칸을 점선으로 표시 | "예상 5 (충돌)" = 실제 5 | 연쇄 전류의 전이 대상은 아직 미리보기에 없음 |
| 5 | 방에 들어가자마자 늘 발각 → 탐사 중 판단이 없음 | 감지 거리 ≥ 배치 거리 | 감지 거리 축소, **순찰/방심** 구분(방심한 적은 고요 단계에서 건드리기 전까지 모름), 기습 +2 | 늑대·사수를 기습으로 처리, 순찰병 동선을 보고 움직이게 됨 | 시야각(등 뒤) 개념은 없음 |
| 6 | 사수가 곁에 붙어도 그대로 발사 | 조준 해제 조건이 시선·제어뿐 | 근접 타격도 조준을 끊음 | 근접 대원의 대응 수단이 생김 | — |
| 7 | 아라의 받아치기가 모든 근접 적에게 매번 발동 | 횟수 제한 없음 | 적 턴당 1회 | 반격 태세 카드의 가치가 살아남 | 회랑 일반전은 아라에게 여전히 쉬움(학습 지역으로 허용) |
| 8 | 수문장전 피해 3으로 끝남 — 예고만 피하면 위험이 없음 | 수호자가 예고 공격만 함 | 곁에 붙어 있으면 예고 없는 **후려치기**(근접, 방어·반격 가능), 소환·방출 예고 중 빈틈이 내 턴까지 남도록 수정 | 자동 검사로 확인. **수정 후 수문장전은 사람이 다시 플레이하지 않았다** | 수호자 난이도 재검증 필요 |
| 9 | 새 게임 직후 이전 진행으로 되돌아감 | 새로고침 직전 `beforeunload` 자동 저장이 교체된 기록을 덮음 | 교체·복원·가져오기 뒤 저장 잠금 | 새 게임 → 금화 10·원정 0, 복원 → 금화 122·원정 5 | — |
| 10 | 좁은 창에서 상단바 글자가 세로로 깨짐, 길드 패널이 시설을 가림 | 줄바꿈 허용, 패널이 캔버스 위에 겹침 | 상단바 nowrap·축약, 패널이 열리면 무대가 줄어들도록 컨테이너 쿼리 | 900×620·720×560에서 잘림 없음 | 720 폭에서 좌우 HUD가 벽 칸 일부를 덮음 |

## 직접 플레이 기록 (실제 클릭·키 입력)

환경: Claude 데스크톱 앱 내장 브라우저, 1280×800, `http://localhost:8812`, 새 저장. 이동·공격·카드·상호작용·정산·투자는 모두 화면 입력으로 했다. **상황 파악에는 읽기 전용 점검 함수(`__ER_DEBUG.ascii()`)로 방의 글자 지도와 상태를 읽었고**, 3번째 원정에서는 방 연결 구조도 미리 읽었다 — 상태를 바꾸는 주입·순간이동은 하지 않았지만, 사람이 화면만 보고 탐색하는 것보다 길 찾기 불확실성이 낮았다. 자동화 도구가 `Space`를 빈 키로 보내 턴 종료는 보조 키 `T`로 했다.

| 원정 | 대원·지역 | 한 일 | 결과 |
| --- | --- | --- | --- |
| 1 | 아라·회랑 | 입구 채집 → 보급실 전투(반격 태세+방패 올리기, 무피해) → 채굴장(약탈자·사수, 조준→발사로 4 피해) → 가방 6/6 가득("가방 자리 없음", 남은 광석은 현장에 유지) → 귀환 확인창 **취소** 후 재확정 | 시간 22/70, 고목재 4·철광 5 등 입고 → 정산의 "바로 가기"로 **제작 공방 복구**(대장간 등장·등불 점등·새 터 공개) → 큰 배낭 제작 |
| 2 | 노아·회랑(다른 배치) | 정밀 채집 카드(+2) → 뒤잡기(3칸 이동 조건)·밀어치기(상자에 충돌, 예상 5=실제 5) | HP 17/26, 시간 14/70 귀환 → **회수 창고 복구**(가방 7칸→배낭 포함 8칸, 깃발 등장) |
| 3 | 아라·회랑 | 늑대·사수 **기습 처치** → 순찰병 교전 → 봉인 장치 → 쉼터 → 채굴장(철벽→방패 강타) → **수문장**: 휩쓸기 회피 → 돌진 회피(벽 충돌·빈틈) → 소환 → 8턴째 처치, 사망 후 잔상·HP 바 없음 → 인장 회수 → 5개 방을 되돌아 귀환 | 시간 47/70, HP 24/30. 정산: 목표 회수·**잿불 용광로 개방**·의뢰 보상(수문장의 뿔). 새로고침 후 금화 118 유지(중복 없음) → **관측 연구실 복구**(루미 합류·망원경·화분) |
| 4 | 루미·용광로 | 이웃 방 이름 표시(연구실 효과) 확인 → 사냥개 2: 불꽃 탄환·화상 처치, 점화 → **문으로 이탈**(안내: 기회 공격 0·추격 1) → 추격자가 문을 넘어옴 → 서리 못·기본 공격으로 처치 → 귀환 | 시간 ~10/90, HP 18/24 |
| 5 | 루미·용광로 | 채집 후 **원정 포기** | "원정 실패 — 잃은 것: 철광 2", 길드 유지 |
| 6 | 루미·용광로 | 적에게 들키지 않고 봉인 상자 도달 → **판정 시도**(1d20 ≥ 11, 50%) → 9 실패: 기본 물품+시간 +3 → 새로고침·재입력 | 굴림 1회 그대로, 금화·난수 상태 불변 |

## 완료 판정 (브리프 §12)

| # | 항목 | 판정 | 근거 |
| --- | --- | --- | --- |
| 1 | 첫 화면에서 할 일을 알고 첫 출격 | **PASS** | 소개 창·상단 안내·고정 목표·원정의 문, 원정 1 |
| 2 | 감지·이동·추격·준비 공격과 턴 | **PASS** | `!`·순찰·조준선·위험 칸·문 넘는 추격을 화면에서 확인 + 자동 검사 |
| 3 | 카드 비용·범위·미리보기·효과 일치, 3빌드 | **PARTIAL** | 사용한 카드 13종은 예상=실제. 자동 검사 9종. 세 대원 모두 실전 투입했으나 **지역 완주는 아라만**, 연구 카드(16종)는 화면에서 쓰지 않음 |
| 4 | 선택적 주사위(고정·행운·실패·복원) | **PARTIAL** | 화면: 상자 판정 실패·복원 무중복. 고정값·행운·안정·변동 룬탄은 자동 검사만 |
| 5 | 가방 초과·귀환/패배 규칙 | **PASS** | 원정 1(현장 유지), 원정 5(패배 상실), 자동 검사(상자 잔여·내려놓기) |
| 6 | 첫 귀환 투자와 다음 출격의 변화 | **PASS** | 공방→배낭→8칸 가방, 외형 변화 3단계 확인 |
| 7 | 첫 지역 완료 → 후속 지역 진입 | **PASS** | 원정 3·4 |
| 8 | 새로고침·새 게임·복원·손상·중복 정산 | **PARTIAL** | 새로고침(전투 중 포함)·새 게임·백업 복원·탭 충돌 PASS. 손상 감지는 DB 기록을 일부러 훼손한 fixture로 확인. **파일 내보내기/가져오기는 화면에서 못 해 봄**(텍스트 왕복은 자동 검사) |
| 9 | 창 크기·겹침·죽은 적 HP 바·호버 | **PARTIAL** | 1280×800·900×620·720×560 확인. 죽은 적 표시·발밑 타원 선택 확인. 720 폭에서 HUD가 벽 칸을 덮음. 4K·초광폭은 미확인 |
| 10 | 보스 처치·귀환·투자·재출격을 실제 입력으로 | **PASS** | 원정 3→관측 연구실→원정 4 |

자동 검사: `npm test` 25개 통과(1차 시점, 현재 28개)(주사위·난수 분리·지역별 150시드 생성 검증·재현성·미리보기=실제·행동 구조·급사 방지·조준 해제·밀치기·수호자·가방·상자 판정·추격·붉은달·정산·투자 경로·재료 획득/소비처·저장·봇 108원정·기습·위험 지형·성장 반영). 봇/결투 도구는 fixture이며 사람의 승률이 아니다.

**측정하지 않은 것**: 10시간 플레이, 2·3지역 수호자전과 완주, 추적자와의 실전, 2·3단계 시설·카드 강화·훈련의 화면 조작, 빠른 연출 옵션, 소리(합성음이 나는지 귀로 확인 못 함), Chrome/Edge 실물과 `file://` 포터블 저장.

## 2차 개선 (2026-09-17, 자체 점검 3라운드)

사용자 요청("스스로 개선점을 찾아 3라운드")에 따라 진행. 측정에는 새로 만든 `tools/smart-bot.cjs`(대원별 스타일을 아는 전투 봇, fixture)를 썼고, 각 라운드는 실제 화면 입력으로 다시 확인했다.

| 라운드 | 관찰 | 원인 | 수정 | 확인 |
| --- | --- | --- | --- | --- |
| R1 전투 균형 | 봇 측정: 루미가 수호자를 잔여 HP 94~100%로 견제, 노아는 사냥개전 잔여 36%, 사제는 기본 덱으로 0~27% 승 | 수호자 후려치기가 이동 **전**에만 나가 달아나는 상대를 못 침 / 노아는 기동 외 방어 수단이 없음 / 사제 체력 과다(27턴) | 수호자: 이동 **뒤** 곁에 닿으면 후려치기 + 4칸 넘게 달아난 상대에게 **격노 질주**(이동 +2). 노아: 3칸 이상 움직인 턴에 첫 근접 피해 −2(**회피**). 사제 체력 66→58 | 봇: 수문장전 잔여 HP 아라 66%·노아 57%·루미 72%, 사냥개전 노아 66%. **직접 플레이**(노아, 성소 앞까지는 fixture 이동): 5턴에 수문장 42→11, 노아 26→20 — 도약→뒤잡기 6, 돌진 유도→빈틈에 뒤잡기 8, 회피 −2 확인 |
| R2 전투 가독성 | 적 턴에 무슨 일이 있었는지 화면에서 안 읽힘. 연쇄·기폭의 2차 대상이 미리보기에 없음. 큰 수호자가 윗칸의 영웅을 가림 | 기록이 모달 안에만 있음 / 2차 효과가 미리보기 계산 밖 | 좌하단 **최근 기록 3줄**, 적 턴 뒤 **요약**("받은 피해 3 · 막음 4 · 회피"), `V` **위협 범위 표시**(격노 질주 포함), 연쇄 전이 대상·피해와 기폭/번지는 불의 번짐 대상을 미리보기에 표시, 손패 가득 참 안내, 수호자 크기 축소 + 가려진 영웅 반투명 덧그림 | 화면에서 기록 피드·요약·위협 범위·안내줄 확인. 전이 미리보기=실제는 자동 검사 |
| R3 원정 중 빌드 변화 | 출격 후 덱이 전혀 변하지 않아 원정 중 "발견"이 없음. 위험 보물고에 갈 이유가 재료뿐 | 원정 내 카드 획득 경로 없음 | **발견 카드**: 보물고 금고(항상)·봉인 상자 판정 **성공** 시 임시 카드 3장 중 1장(또는 금화 5). 미연구 카드 우선, 그 원정에만 유효, 손패로 바로 들어옴. 정산에 "써 본 발견 카드"와 연구 비용·**연구 목표로 고정** 버튼 | **직접 플레이**(보물고까지 fixture 이동): 금고 개봉 → 3장 제안 → 변동 룬탄 선택 → 숨 고르기로 뽑아 사용(예상 3~6, 굴림 3, 사수 처치) → 추격자 처리 후 귀환 → 정산에 연구 안내 표시 |

이번에 새로 확인된 항목: 주사위 **카드**(변동 룬탄)의 화면 동작 → 완료 판정 4번은 "행운·안정·고정값만 자동 검사"로 범위가 줄었다. 자동 검사는 28개.
여전히 남은 것: 2·3지역 수호자전 직접 플레이, 집행자·사제는 기본 덱 봇 기준 잔여 HP 31~51%로 빡빡함(성장 조건에서는 55~94%), 발견 카드 풀의 지역별 차별화 없음.

## 외부 플레이 (2026-09-17)

단일 HTML을 claude.ai Artifact로 게시했다: https://claude.ai/artifact/JtDzd9h3Wo5PMyG6u8oFtt (비공개 기본). 게시 환경은 파일 다운로드를 막으므로 저장 내보내기/가져오기에 텍스트 복사·붙여넣기를 추가했다. 게시본은 출처가 달라 로컬 저장과 공유되지 않는다. **게시된 페이지를 실제로 열어 플레이해 보지는 못했다**(로컬에서 같은 빌드는 확인). 휴대폰 폭에서는 조작이 비좁다 — PC 권장.

공개 주소: https://doridori0150.github.io/emberwake-reborn/ (GitHub Pages, 공개 저장소 `doridori0150/emberwake-reborn`). 게시 직후 첫 화면 로드와 콘솔 오류 없음을 확인했다.

## 3차 수정 (2026-09-17, 사용자 피드백 4건)

| 요청 | 조치 | 확인 |
| --- | --- | --- |
| 캐릭터 머리 위 HP, 특수 자원과 캐릭터별 특수기 | 영웅 머리 위 체력 바·수치·방어·투지 점. **투지**(최대 5) + 대원별 특수기(`C`, 투지 3 + 주/보조 행동) | 자동 검사(획득 조건·소모·효과·탐사 중 불가), 화면에서 HUD·버튼·머리 위 표시 확인. **특수기 발동 자체는 화면에서 눌러 보지 않았다** |
| 발견했는데 안 움직이는 고블린 | 봇 1,473 적 턴 재현: 경계 중인 근접 적이 멈춘 2건은 모두 영웅이 적 턴 도중 쓰러진 경우 → 이동 버그 아님. 원인은 "방심한 적은 건드리기 전까지 모른다" 규칙 → **시선 규칙**으로 교체(정면 2칸 감지, 6걸음마다 뒤돌아봄, 호버 시 살피는 칸 표시) | 자동 검사 + 화면: 시선 칸 표시, 정면으로 들어가자 발각·걸음 멈춤 |
| 치명타 추가 | 영웅 직접 공격 10%(+빈틈 20, +노아 이동 10), 피해 +50%, 미리보기에 "치명 6 (10%)" 표기, 연출("치명타!"·흔들림), 투지 +1 | 자동 검사(확률 범위·저장 복원 뒤 같은 굴림·적은 치명타 없음), 화면에서 미리보기 표기 확인 |
| 로비에서 캐릭터가 안 움직임 | 선택한 대원이 안뜰을 **직접 걷는다**: WASD/클릭 이동(길 찾기), 시설 곁 `E`, 시설 클릭 시 걸어가서 열기. 다른 대원은 제자리 NPC. 바로가기 버튼 유지 | 화면: WASD 이동, "E · 의뢰 게시판 보기" 안내, 창고 클릭 → 걸어가서 패널 열림 |

자동 검사 30개. 공개 주소와 비공개 게시본에 반영.

## 4차 수정 (2026-09-17, 레벨 구성)

사용자 지적: "강적·상자·봉인 장치·재료·지형이 따로 논다." `mapgen.buildRoom`을 무작위 배치에서 구성 배치로 바꿨다.

- 목표 지점: 모든 문에서 가장 먼 벽가에 상자·봉인 장치·특산/광맥 재료를 모은다. 흔한 재료는 입구 쪽 벽가.
- 경비: 근접·정예는 목표 바로 앞, 사수·술사는 뒤에서 엄호. 목표물에 `guards`(적 id)를 기록하고, 경비가 살아서 3칸 안에 있으면 채집·개봉·장치 작동이 막힌다(`run.guardsNear`). 화면에는 "경비 중" 표시.
- 남는 근접 적 하나는 목표 반대편 통로를 순찰(문에서 3칸 이상). 보급실 약탈자는 상자를 보느라 등을 돌리고 있다(기습 기회).
- 위험 지형은 경비 옆(방 가운데 반대쪽)에 둔다 → 정면에서 밀치면 빠진다. 문 앞은 실제로 있는 문만 비운다.
- 검증: 테스트 31개 통과(지역별 80시드에서 모든 상자·장치 3칸 안에 경비, 막힘/떼어내기/처치 후 개봉). 봇 150회 귀환율은 이전과 같은 수준(아라 44, 노아 83, 루미 119 / 이전 44, 86, 109). 브라우저에서 보급실 배치·"경비 중"·막힘 문구 확인. 이전 버전에서 시작한 원정은 `guards`가 없어 그대로 동작한다.
- 미검증: 2·3지역 방을 손으로 플레이해 보지는 않았다(생성·봇만 확인).

## 5차 (2026-09-17, 데이터화와 콘텐츠 도구)

사용자 결정: 상용 엔진은 나중에, 지금 구조로 완성 후 포팅. 그래서 "수정·추가가 쉬운 형태"를 먼저 만들었다.

- **적 특성 9종**(`data.TRAITS` + `run.js`): 자폭·분열·재생·격앙·정면 방패·지휘·소매치기·저주 화살·둥지. 새 행동 유형 `none`. 새 적 10종과 등장 조합 16개는 전부 `src/content.js`(JSON)에 있다.
- **수제 방**: `content.js`의 `rooms`. `map.lintRoom`(도구·생성기 공용)과 `map.applyRoom`. 첫 묶음 7개(약탈당한 창고, 가시 회랑, 가시 울타리 정원, 슬래그 웅덩이, 방패 전열, 먹물 둥지, 기둥의 결투장).
- **editor.html**: 방 검수 / 방 에디터(+시험 플레이 `index.html?test=1`, 저장 안 함) / 몬스터 작업대(+모의전 `src/bot.js`). 저장은 `tools/server.cjs`의 `/__dev/content`(127.0.0.1 전용, 같은 출처만, 쓰는 파일은 content.js 하나, 본문은 서버가 생성).
- 방 구성 수치는 `RULES.level`로 뺐다.
- 검증: 테스트 33개 통과. 테스트가 실제 설계 실수를 잡았다 — 수제 채굴장에 광석이 없어 공방 1단계 재료가 끊김 → "재료 공급량" 검사를 lintRoom 에 추가. 새 조합 16개 모의전(60판)은 기존 장면과 같은 범위(승률 95~100%). 봇 원정 120회×3지역 교착 없음. 브라우저에서 확인: 방 검수 12장 렌더·에디터로 복사, 에디터에서 적 배치·순찰 지정·시선 변경·위험 지형·지우기, 시험 플레이 2회(회랑 보물고/용광로 방패 전열 — 뒤집기·색 필터·경비 표시 정상), 작업대 수치 변경→"수정됨"→저장→파일 확인→되돌림, 모의전 표.
- 미검증: 새 적과의 전투를 사람 손으로 끝까지 해 보지는 않았다(규칙은 자동 검사, 난이도는 봇). 도구의 좁은 화면 배치는 1100px 이하에서 한 줄로 쌓이는 것만 확인. 파일을 직접 연(`file://`) 읽기 전용 모드는 코드 경로만 있고 눌러 보지 않았다.
- 알려진 한계: 시험 플레이는 실제 브라우저에서는 새 창으로 열린다. 도구에서 등장 조합을 바꾸면 방 검수 탭에는 저장 후 새로고침해야 반영된다. 정면 방패의 방향은 툴팁·피해 미리보기로만 알 수 있다(전용 표시 없음).


## 6차 (2026-09-18, 가방 확장 · 무기 주사위 · 제작 옵션 · 이벤트 · 기본 수치)

사용자 선택: 피해 주사위만(명중 굴림 없음) / 제작 가방 + 주머니 / 대화 + 선택지 + 효과. 모두 `content.js` 데이터이고 도구에서 편집한다.

- 엔진: `run.weapon`·`stackOf`·`gearSum`(옵션 포함)·`fireEvent/doEvent`, `guild.equip`(칸별)·`option` 투자 대상·`removeOption`·`fire/answer`, `events.js`, `dialog.js`. 검사·봇용 실행 옵션 `flatAttack`·`noEvents`.
- 데이터: 무기 6종, 가방 3종(큰 배낭 상향 +2), 제작 옵션 5종, 이벤트 5개(첫 하강 → 다친 정찰병 → 귀환 뒤 보답 연결, 공방의 불, 수문장의 최후). `정밀 타격`은 무기 피해 +2.
- 도구: 제작·장비 / 이벤트(+초상화 올리기 `/__dev/portrait`, 미리보기) / 기본 수치 탭. 되돌리기·검사 훅 공용. 포터블 빌드는 초상화 그림도 내장한다.
- 검증: 테스트 36개(무기 범위·치명타 추가 주사위·전용 무기, 가방·주머니·옵션 칸, 이벤트 트리거·조건·효과·플래그 연결·길드 이벤트). 전투 봇 승률은 이전과 같은 범위. 브라우저: 원정 시작 대화(초상화·2쪽·Enter), 이전 저장의 원정 이어하기, 공방에서 가방 제작→옵션 붙이기, 도구 세 탭 편집·되돌리기·미리보기, 초상화 API(정상/잘못된 id/가짜 그림 거부).
- 도구를 처음 열 때 이벤트 목록이 지워지는 버그를 저장 전에 발견해 고쳤다(같은 배열을 비우던 문제).
- 미검증: 실제 그림 파일을 파일 선택 창으로 올리는 동작(API만 확인), 선택지 있는 이벤트를 게임 화면에서 끝까지 고르는 흐름(규칙은 테스트, 대화창은 도구 미리보기로 확인), 새 무기를 낀 실제 전투. 검증용으로 내 시험 저장(localhost:8812)의 공방 단계·재료를 직접 올렸다(fixture).
- 알려진 한계: 길드 이벤트의 확률은 재현되지 않는다(Math.random). 무기 아이콘은 검 하나를 같이 쓴다. 이벤트 효과에 "장비·카드 영구 지급"은 아직 없다.


## 7차 (2026-09-18, 문라이터식 로비: 마을·주민·가게·낮밤)

사용자 선택: 걸어 다니는 마을 + 주민 대화 + 가게 운영 + 낮/밤, 시설은 NPC에게 말 걸어 열기.

- `guildview.js`를 마을 화면으로 다시 썼다(30×20, 카메라, 건물·나무·길, 주민 5명 + 다른 대원, 행인, 화면 밖 시설 방향 표시, 해 질 녘 조명). `data.TOWN`·`data.NPCS`, content `npcs`, 이벤트 트리거 `npcTalk`.
- `guild.shopDay/newDay/dayPhase/quickPrice`, `RULES.shop`. 앱: 주민 대화 → 패널, 가게 진열·장사 재생·결산, 상단에 "N일째 낮/해 질 녘". 급매는 60%.
- 도구: "마을" 탭(주민 편집·지도에서 자리 지정·말 걸어 보기·주민 이벤트 만들기), 기본 수치에 가게 규칙 8개.
- 검증: 테스트 37개. 브라우저: 마을 렌더, 바로가기로 창고지기에게 걸어가 대화 → 복구 → 다시 말 걸어 가게 열기 → 진열(철광 5@3, 고목재 5@3) → 손님 8명 재생(고목재 3금은 전원 거절, 철광은 만족/망설임) → 결산 +12, 수첩·수요 기록, 해 질 녘 전환. 도구 마을 탭에서 자리 옮기기·되돌리기·미리보기.
- 주의: 이 미리보기 창은 화면이 가려지면 애니메이션 프레임이 멈춰서, 걷기는 디버그용 `__ER_DEBUG.town().tick`을 직접 돌려 확인했다(실제 브라우저에서는 정상 속도).
- 미검증: 키보드(WASD)로 마을 전체를 돌아다니는 체감, 행인 움직임, 창 크기별 배치. 나무·풀은 코드로 그린 임시 그림이고 주민은 대원 스프라이트에 색 필터를 입힌 것이다 → Codex(Astra)에 이미지 생성을 요청해 `assets/generated/`로 받는 중.


## 8차 (2026-09-18, 중간 정리 — Codex(Astra) 리뷰와 생성 에셋)

- 사용자 질문: "AI가 휘갈겨 쓴 코드라 나중에 고치기 더 힘든 것 아닌가, 엔진을 바꿀 거면 의미 없는가." → Codex(gpt-6-astra, high)에 읽기 전용 리뷰를 맡겼다. 결론 "버릴 코드는 아니지만 정리 먼저". 기록·반영표: `docs/codex-requests/2026-09-18-cleanup-review.md`.
- 리뷰가 재현해 낸 실제 결함(내 자체 평가가 놓친 것): 도구에서 0 으로 만든 값이 게임에서 되살아남, "저장됨" 오판, 지워진 장비를 가리키는 저장의 예외, 서버 경로 검사 허점, 빌드 이스케이프 무효. 전부 고치고 회귀 테스트 4개 추가(41개).
- 정리 순서: 결함 수정 → 포매팅 전용 커밋(3,110줄 → 14,405줄: 한 줄에 몰아 쓴 코드가 풀린 실제 크기) → `doCard` 분해·이벤트 절차 공용화. 각 단계에서 테스트와 봇 통계 출력이 이전과 글자 단위로 같은지 확인했다.
- 같은 실수 3회(한 줄 코드에 `//` 주석을 끼워 뒤 코드를 삼킴 — 이번엔 서버 저장 응답이 사라져 요청이 멈춤). 포매팅으로 한 줄 과밀 코드가 없어져 재발 조건이 줄었고, 이후 수정은 문자열 치환 대신 일반 편집으로 한다.
- 포팅 관점(리뷰 H): 가져갈 것 = content.js·data.js 의 표, 테스트, 문서. 명세로 쓰고 다시 짤 것 = rng/run/guild/events/mapgen. 버릴 것 = app/render/guildview/dialog. 그래서 화면 코드의 큰 함수 분해는 뒤로 미뤘다.
- 에셋: Codex 이미지 생성으로 25장(주민 초상화·서 있는 그림 각 5, 나무 2·덤불·잔디 타일 2, 판매대·선반, 무기·가방 아이콘 8)을 받아 `tools/process-generated.py` 로 줄이고(`assets/town/`, 1.4MB) `tools/town-assets.cjs` 로 매니페스트에 등록. 마을 잔디·나무·주민, 대화 초상화, 가게 판매대, 장비 아이콘에 연결. 원본(`assets/generated/`, 35MB)은 git 에서 제외. AI 생성 그림이므로 상용 출시 전에 권리·화풍 통일을 다시 검토할 것.
- 미검증: 선반·덤불 그림은 아직 어디에도 쓰지 않았다. 주민 그림은 대원(2.5등신)보다 등신이 높아 나란히 서면 화풍 차이가 보인다.


## 9차 (2026-09-18, 던전 기믹 · 귀환 편의)

이전 요청 중 남아 있던 두 가지("던전에 기믹", "귀환할 때 되돌아가는 게 귀찮다")를 처리했다.

- 엔진: `explodeBarrel`/`fusedBarrels`/`pushSpot`/`pullLever`, 타일 `s`(잠든 가시), 상자의 시야 차단(`los`), 밀치기 미리보기에 폭발통 피해 포함. `act({t:"travel"})`·`travelInfo`·`act({t:"recall"})`, 수호자 처치 시 귀환 균열. 장비 효과 `recall`, 장신구 귀환석. 규칙 수치 `RULES.gimmick`.
- 생성기: 적 있는 방 4,200개 중 폭발통 498 · 상자 293 · 레버 687(3지역×200시드). 모두 도달 가능성 검사를 통과.
- 화면: 기믹 그림(폭발통은 상자 그림에 붉은 필터, 레버·잠든 가시는 도형), 지도에서 방 클릭 이동, 귀환석 버튼(K). 도구: 방 에디터에 폭발통·밀 상자·레버·잠든 가시 도구, 기본 수치에 기믹 규칙.
- 시험 플레이 버그 2건 수정: 그 지역 미궁에 없는 방 종류(회랑의 hall)를 고르면 시작 실패 → 방 하나를 그 종류로 바꿔 준다. 시험 플레이에서 "첫 하강" 이벤트가 뜨던 것 → 끔.
- 검증: 테스트 43개(기믹 미리보기=실제 피해, 연쇄 폭발, 상자 시야, 레버 피해, 자동 이동 시간, 균열, 귀환석). 봇 원정 100회×3지역 교착 없음(아라 귀환 58/49/34). 브라우저: 기믹 시험실 렌더, 레버 선택지 문구, 지도에서 실제 클릭으로 입구 방까지 자동 이동(시간 +2).
- 미검증: 폭발통에 불붙이기·상자 밀기·레버를 화면에서 직접 눌러 보지는 않았다(규칙은 테스트). 기믹 전용 그림은 없다(임시).


## 10차 (2026-09-18, 던전 세틀러즈 리서치 반영: 지역 소진·탈출 경고·배치식 마을)

- 리서치 결론(채팅에 정리): 맵·시야·전술 전투·정착지 순환이 우리와 겹치고, 실시간 4인·퍼머데스·칸 단위 건설이 다르다. 가져온 것: 소진 카운터 → 지역 소진·재생, 탈출 판단 → HUD 경고, 정착지 → 배치식(덩어리) 건설 + 인접 보너스.
- 엔진: `guild.wearOf/yieldOf/newDay`, 생성기 `opts.yield`(재료 총량 감소), `guild.zoneOf/spotOf/canPlace/moveBuilding/placeDecor/moveDecor/removeDecor/townBonus`, 제작·훈련 비용 할인, 가게 손님·값 보너스. 데이터: `TOWN.places[].size/movable`, `DECOR` 11종(content `decor` 로 덮어쓰기 가능), `RULES.town/deplete/warn`.
- 화면: 마을이 `G.layout`을 따라 그려지고 주민이 시설을 따라간다. "마을 꾸미기" 모드(격자·부지 표시·집기·놓기·내려놓기·치우기, Esc), 장식 목록에 비용·조건, 배치 보너스 현황. 준비 화면 소진 표시, 원정 HUD 탈출 경고. 도구: 기본 수치에 배치·소진 수치 10개.
- 검증: 테스트 45개(소진 누적·회복·상한·수제 방 배율, 배치 겹침·문 앞·마을 밖, 인접 보너스가 실제 비용에 반영, 장식 비용·치우기, 옛 저장 호환). 브라우저: 탈출 경고 표시, 소진 기록(원정 뒤 verdant 1), 꾸미기에서 짐 상자 놓기(재료 차감)와 공방을 창고 옆으로 옮겨 제작 비용이 줄어드는 것, 주민이 따라 이동. 재료가 없는 시험 저장이라 재료를 임시로 넣어 확인했다(fixture).
- 미검증: 장식 집어 옮기기·치우기를 화면에서 직접 클릭으로는 안 해 봤다(규칙은 테스트). 마을 꾸미기 바가 좁은 화면에서 게임 화면을 얼마나 가리는지.
- 다음: 배치 2단계(길 연결·주민 출근·장식 종류별 효과), 지역 소진의 수치 밸런스는 플레이 데이터 보고 조정.


## 11차 (2026-09-18, 플레이 테스트 준비 2라운드: 행동 타입·티어 · 적 패턴 3종 · 진단 번들)

- Codex 1라운드 리뷰(`docs/codex-requests/2026-09-18-playtest-round1.md`) 반영: 광역·돌진 공격도 적 턴 공격 예산(`areaHit`·`actBoss` charge)을 쓴다, 수호자 예고 칸 + 접근 범위를 함께 표시(`threatTiles`), 빈 원정으로 날짜를 넘기는 우회 차단(`RULES.dayMinTime`), 철갑병(heavy)은 곁에서 약타 견제 + 다음 턴 전방 2×3 깊은 내려찍기.
- 행동 타입 4종(발더스 3식): `CARDS[].slot` = main 주 행동 · bonus 보조 행동 · **reaction 반응**(내 턴에 걸어 두면 적 턴에 조건 사건 1회 발동, 주·보조를 쓰지 않음; 반격 태세) · **free 자유**(탐사 준비, 같은 카드 턴당 1회; 정밀 채집·탐색 도면·해체 요령). 티어 `tier` 1 기본/대원 · 2 연구 1단계 · 3 연구 2단계(자동 배정). **T3 충전**: `charge: 1` 카드(연막·지면 강타)는 원정당 공용 충전 `RULES.charges`(2)를 쓰고, 야영에서 체력 회복 대신 충전 +1. 엔진: `slotReason(run, slot, card)`/`costText`/`spend`, `hurtHero` 앞단에서 반응 발동(그 공격 1회에만 방어·되치기), `beginHeroTurn` 초기화, 야영 `recharge`. 화면: 카드 배지 4색 + `T#`·충전 표시, HUD "반응: 카드명"·충전 핍, 첫 전투 안내 창(한 번).
- 적 패턴 3종(데이터 `ai` 값으로 도구에서 배정): **lunge 도약**(재의 사냥개: 2~4칸에서 웅크렸다가 내가 선 자리로 도약, 1칸 비키면 따라 물림, 2칸 넘게 벗어나야 헛물·빈틈) · **shield 방패병**(방패 파수병: 예고 없는 약타로 1칸 밀치기, 등 뒤가 막히면 찧기로 온전한 피해) · **firepot 투척병**(잿불 투척병: 조준 후 십자에 잿불 단지, 맞은 자리는 `RULES.fire.turns`턴 불타고 밟으면 `RULES.fire.dmg`). 불길(`rm.fire`)은 영웅 경로가 목적지일 때만 밟고 적은 피해 가며, 전투가 끝나거나 방을 떠나면 꺼진다. 조준류(`aiming`)는 근접 타격·속박·연막으로 끊긴다.
- 플레이 테스트 키트: 설정 → **진단 정보 복사**(버전·커밋·콘텐츠 해시·시드·최근 행동 40개·저장; 복사가 막히면 글상자), `src/build-info.js`(빌드가 생성, Pages 용으로 커밋), 정산 뒤 "지난 원정 기록"(`lastReport.log`), 귀환 확인 창에 기회 공격 위험 표시, `tools/make-save.cjs`(캠페인 봇 진행 저장 → `dist/test-saves/late-N.json`, 저장 관리에서 가져오기).
- 경제: **행상**(`guild.market/buyMat`, `RULES.shop.buyMul/buyKinds/buyQty`) — 창고가 있으면 날마다 열린 지역 재료 3종을 기준가 3배에 2개씩. 금화 소비처.
- 봇: 캠페인 봇이 만든 장비를 쓸 수 있는 대원 모두에게 끼우고, 모자란 재료를 행상에서 사며, 수호자 방의 귀환 균열로 나가고, 추적자에게 구석에 몰려 60라운드 동안 피해 교환이 없으면 포기한다. 교착 덤프에 적·불길·손패·기록을 포함. 찾은 버그: 귀환 균열이 문 앞 칸에 생겨 문을 막음 → `freeNear(..., {keepDoors})`.
- 검증: 테스트 51개(반응·자유·충전·야영 충전, 적 패턴 3종, 행상). 캠페인 4회×100원정 예외 0, 1회 완주(42원정·패배 0), 나머지 3지역 성소 미도달(기존 위험). smart-bot 승률 변화 없음(용광로 일반전은 사냥개 도약을 봇이 잘 피해 잔여 HP 상승). 브라우저: 준비 화면 카드 배지 `[반응 · T1]`/`[자유 · T1]`, HUD 충전 핍·반응 표시, 첫 전투 안내, 진단 정보 글상자, 시험 방에서 투척병 조준 십자·불길 연출·사냥개 웅크림 확인.
- 미검증: 행상 창은 규칙 테스트만(창고 0단계 새 게임에서는 선택지가 없음), 방패병 밀치기 연출, 반응 발동 순간의 화면 문구.

## 12차 (2026-09-18, 플레이 테스트 준비 3라운드: Codex 2라운드 리뷰 반영)

- 리뷰 기록 `docs/codex-requests/2026-09-18-playtest-round2.md`(요청·답변·반영표). 차단 5건 전부 반영: 반응 방어 잔량 누출(별도 값·1회 소모), 도약 표시=피격, 투척병 시야·예산 재검사, 시간 0 포기의 날짜 우회(`run.gaveUp`), 봇의 서고 보스 도전 소멸(지역별 번갈아 도전) → 캠페인 4회 중 2회 완주(목표물 회수 포함).
- 권고 반영: 전투 시작 시 `freeUsed`·반응 초기화, 손패 툴팁·안내줄에 사용 불가 사유, 불길 진입 판정 통일(밀린 적도 피해, `freeNear` 불길 제외, 툴팁), 봇이 반응 뒤 주 행동을 이어 감, smart-bot 혼합 장면 2개, 진단 번들(시드·예외·저장 방식·세션 유지·확인창), README 테스터 안내(제보 경로·첫 과제·예시·`saves/late-30.json`), 테스트 "수호자 격파 보증".
- 검증: 테스트 53개. smart-bot: 아라 수문장 잔여 HP 76%(반응+공격 동시), 방패 전열/혼합 lumi 78~80%. 캠페인 4×100: 예외 0, 완주 2(89·37원정).
- 미반영(플레이 데이터 뒤): 충전 카드 절약 체감, 행상 수치, 캠페인 단계별 계측, 난이도 선택·세이브 슬롯.

## 남은 결함·위험

- 난이도: 회랑 일반전은 아라에게 쉽다. 수문장은 2차 개선 R1에서 노아로 다시 플레이해 확인했지만(5턴까지), 아라·루미와 2·3지역 수호자는 봇 수치뿐이다. 3지역 성소 도달률이 낮고(시간 제한), 노아가 후반에 약하다. 금화는 행상(11차)으로 쓸 곳이 생겼지만 수치는 미조정.
- 적 걷기 애니메이션은 1프레임+들썩임, 적은 좌우 2방향뿐(원화 한계). 2·3지역 적은 1지역 스프라이트의 색조 변형이다.
- 마을은 걸어 다니고 시설 이동·장식 배치가 된다(10차). 길 연결·주민 출근 같은 배치 2단계는 없다.
- 포터블 HTML은 개발 서버 경로로 띄워 확인했다(같은 출처라 저장이 공유됨).

## 에셋 현황

대부분 참조 프로젝트의 기존 그림을 재사용했다. 8차에 Codex 이미지 생성으로 마을 에셋(`assets/town/*.webp`, `town.*` id)을 만들었고, 던전 기믹(폭발통·상자·레버)과 불길은 코드로 그린 임시 그림이다.
- 그대로 맞는 것: 대원 3명(4방향 대기·보행·공격·스킬 32프레임), 적 6종 원화, 타일·기둥·상자·귀환문, 재료 아이콘 12종, 시설 단계 그림, 장식, 카드 아이콘.
- **임시·전용(교체 권장)**: 달빛 결정(수정 아이콘 색조 변형), 목표물(고대 부품 아이콘), 봉인 장치(world.seal), 장비 아이콘 일부(해체 도구=나침반, 랜턴=신호기, 촉매 반지=강장제, 수문장의 뿔=붕대), 2·3지역 적·수호자 전부(색조 변형), 위험 지형 3종·덫(코드로 그림), 폐허 시설 터(상자·벤치를 어둡게), 지역별 옆·아래 벽(윗벽을 어둡게).

## 보류 (2026-09-18)

소유자 결정으로 여기서 멈추고 킵한다. 마지막 상태: 커밋 `3eed52b`(코드) + `f8508cf`(빌드 정보), Pages·Artifact 게시본 모두 같은 빌드. 테스트 53개 통과, 캠페인 봇 4×100원정 예외 0·완주 2. Codex 리뷰 3라운드 기록은 `docs/codex-requests/`에 요청·답변·반영표로 남아 있다.

## 재개 시 바로 할 일

1. 환경 확인: `npm test`(53개) → `npm start` → `게임 실행.bat`/`에디터 열기.bat`. 원본 `EmberwakePrototype`(포트 8791)은 건드리지 않는다.
2. 사람 플레이 테스트: README "테스터 안내"대로 5명 이내 제한 테스트. 제보의 진단 정보(설정 → 진단 정보 복사)로 시드·행동을 재현한다. 후반은 `saves/late-30.json`.
3. 플레이 데이터 뒤 수치 조정: 3지역 성소 도달 시간(서고 limit 110→?), 노아 후반, 행상 가격(`RULES.shop.buy*`), 충전 카드 체감(`RULES.charges`). 전부 도구의 "기본 수치" 탭에서 바꿀 수 있다.
4. Codex 2라운드 미반영 권고: 캠페인 검사 단계별 분리(장치→성소→격파→회수→생환), 난이도 선택·세이브 슬롯, 사수 감시선·예고 소환 패턴, 방패병 뒤쪽 위험 표시.
5. 임시 에셋 교체(기믹·불길·2·3지역 적): ID만 유지하면 저장은 영향받지 않는다.
6. 엔진 포팅을 결정하면 `docs/codex-requests/2026-09-18-cleanup-review.md` H 항목(그대로 가져갈 것/명세로 쓸 것/버릴 것)부터 본다.
