# Emberwake Reborn — 잿불의 귀환 (v0.1.0)

망한 길드를 되살리는 **단독 모험가 미궁 탐사 덱빌딩**. 오프라인 싱글 플레이 웹 게임(HTML + Canvas, 외부 라이브러리·네트워크 없음).

> **상태(2026-09-18): 보류.** 플레이 테스트 가능한 상태(3라운드, 커밋 `3eed52b`)에서 개발을 멈추고 킵한다. 재개할 때는 [docs/WORKLOG.md](docs/WORKLOG.md)의 "재개 시 바로 할 일"부터 본다.

> 길드에서 투자 목표를 정한다 → 대원·덱·장비·목적지를 준비한다 → 미궁에서 탐사·전투·채집한다 → **더 들어갈지 지금 돌아갈지** 정한다 → 귀환문을 지나야 전리품이 남는다 → 시설·장비·카드·대원에 투자한다 → 길드와 다음 원정이 달라진다.

참조 프로젝트 `../EmberwakePrototype`(v0.16.1)와는 **폴더·앱 ID·IndexedDB·포트가 모두 다르다.** 원본의 게임·저장·배포물은 읽기만 했고 수정하지 않았다.

| 항목 | 값 |
| --- | --- |
| 앱 ID / DB | `emberwake-reborn` / IndexedDB `emberwake-reborn-db` |
| 개발 서버 | `http://127.0.0.1:8812/` (원본은 8791) |
| 포터블 빌드 | `dist/EmberwakeReborn-Portable.html` (단일 파일, 약 9.5MB, `npm run build`) |
| **공개 플레이 주소** | https://doridori0150.github.io/emberwake-reborn/ (GitHub Pages, 누구나 접속 · 저장소 `doridori0150/emberwake-reborn`의 main에 push하면 자동 갱신) |
| 웹 게시본 | https://claude.ai/artifact/JtDzd9h3Wo5PMyG6u8oFtt (기본 비공개, 페이지의 공유 메뉴에서 공개 범위 설정). `npm run build`가 만드는 `dist/EmberwakeReborn-Artifact.html`을 다시 게시해 갱신한다. 게시본에서는 파일 내려받기가 막혀 저장 이동은 **저장 관리 → 텍스트로 복사·붙여넣기**를 쓴다 |

## 실행

```bash
npm start          # http://127.0.0.1:8812/  (node tools/server.cjs, 의존성 설치 불필요)
                   # 콘텐츠 도구: http://127.0.0.1:8812/editor.html
npm test           # 규칙·생성·경제·저장·콘텐츠 자동 검사 51개
npm run build      # dist/EmberwakeReborn-Portable.html 생성
npm run sim -- verdant ara 200 loot   # 봇 원정 통계(난이도 근사치)
node tools/smart-bot.cjs 100 [grown]   # 대원별 스타일 전투 봇: 장면별 승률·잔여 HP(균형 방향 확인용)
node tools/campaign.cjs 3 100 [verbose]   # 새 게임부터 3지역 수호자까지 전체 루프 봇(교착·경제 병목 탐지)
node tools/make-save.cjs 30 7  # 캠페인 봇으로 진행한 시험용 저장 → saves/late-30.json (저장 관리 → 파일에서 가져오기)
npm run assets     # 참조 프로젝트에서 에셋 다시 가져오기 + 매니페스트 재생성
```

포터블 HTML은 Edge/Chrome에서 바로 열면 된다. `file://`에서는 브라우저 정책에 따라 IndexedDB가 막힐 수 있으며, 그때는 localStorage로 자동 대체된다(저장 관리 창에 표시). 주소·브라우저를 옮길 때는 **저장 관리 → 파일로 내보내기/가져오기**를 쓴다.

## 조작

| 입력 | 기능 |
| --- | --- |
| (길드) WASD·클릭 / `E` / `Enter` | 안뜰을 걸어 다니기 / 곁의 시설 열기 / 원정 준비. 시설을 클릭하면 걸어가서 연다 |
| 칸 클릭 / 방향키·WASD | 이동 (탐사 중 자유, 전투 중 이동력 소모) |
| 적 클릭 / `F` → `Tab` → `Enter` | 기본 공격 (방심한 적을 먼저 치면 기습 +2) |
| `1`~`7` / 카드 클릭 | 카드 선택 → 대상 클릭 또는 `Tab`·`Enter`. 자신 대상 카드는 한 번 더 누르면 사용 |
| `G` / `R` | 기본 방어(주 행동) / 숨 고르기(보조: 한 장 버리고 한 장 뽑기) |
| `C` | 대원 특수기(투지 3 소모) |
| `E` `Q` `Z` `X` | 곁의 소품 상호작용 — 하단 안내줄에 대상·키·비용 표시. 야영: 체력 회복 또는 충전 +1 |
| `Space` 또는 `T` | 턴 종료 |
| `V` | 위협 범위 표시(다음 적 턴에 공격이 닿는 칸) |
| `M` `B` `L` | 지도 / 가방 / 기록 |
| `Esc` / 우클릭 | 선택 취소, 창 닫기 |

## 핵심 규칙 요약 (자세한 내용은 [docs/DESIGN.md](docs/DESIGN.md))

- **턴 = 이동력 + 주 행동 1 + 보조 행동 1.** 카드 배지가 행동 종류다: **주 행동** / **보조 행동** / **반응**(걸어 두면 적 턴에 조건이 맞을 때 1회 발동, 주·보조를 쓰지 않음) / **자유**(탐사 준비, 같은 카드 턴당 1회). 카드가 없어도 기본 이동·공격·방어는 항상 가능하다.
- **티어와 충전**: 카드의 `T1~T3`는 기본 / 연구 1단계 / 연구 2단계. `충전 1`이 붙은 T3 카드(연막·지면 강타)는 원정당 공용 충전 2를 쓰고, 야영에서 체력 대신 충전 +1을 고를 수 있다.
- 내가 턴을 끝내야 적이 움직인다. 탐사 중 이동은 시간이 들지 않고, 시간은 방 이동(2)·채집(1)·상자(1/안전 3)·카드(1)·휴식(4)·전투 라운드(1)에만 흐른다.
- **붉은달 게이지**: 50% 술렁임(적 감지 +1, 처음 가는 방에 적 +1) → 80% 붉은달(시야 감소, 적 피해 +1, 달빛 결정 출현) → 100% 추적자 등장. 귀환문은 끝까지 열려 있다.
- 적은 **예고**한다: 사수는 조준 후 발사(시야 차단·근접 타격·제어로 풀림), 철갑병·수호자는 위험 칸을 한 턴 전에 표시. 예고 없는 공격(철갑병의 약타, 방패병의 밀치기)은 방어로 받아낸다. 사냥개는 웅크렸다가 내가 선 자리로 도약(2칸 넘게 벗어나야 헛물), 투척병의 잿불 단지는 맞은 자리를 2턴 불태운다. 한 적 턴에 실제 공격은 최대 2회(광역·돌진 포함), 원거리는 1회.
- 덱 12장·같은 카드 2장·시작 손패 5·턴당 2장·손패 한도 7. 덱이 돌 때마다 시간 +1.
- 주사위는 **일부 특수 카드와 상자·제단 판정에만** 쓴다. 굴리기 전에 비용·성공률·실패 결과를 보여주고, 실패해도 기본 보상은 받는다.
- 가방은 재료별 묶음 칸(기본 6칸). 넘치는 물품은 그 자리에 남는다. **귀환해야 창고에 들어가고 패배하면 가방을 잃는다.** 복구한 길드는 남는다.
- **투지와 특수기**: 전투 중 내 턴 시작·처치·치명타마다 투지 +1(최대 5), 대원별 조건으로도 +1(아라: 방어로 막아냄 / 노아: 한 턴 3칸 이동 / 루미: 화상·중독 부여). 투지 3으로 특수기 — 아라 `수호의 함성`(보조: 방어 6·반격 3), 노아 `그림자 난무`(주: 3칸 안 적 곁으로 순간이동해 피해 5, 이동 +2), 루미 `달빛 폭주`(주: 범위 피해 2·화상 2). 주/보조 행동을 함께 쓰므로 무료 추가 행동이 아니다.
- **치명타**: 영웅의 직접 공격만 10%(빈틈 상태의 적 +20%, 노아가 3칸 이상 움직인 턴 +10%)로 피해 +50%. 확률과 치명 피해는 미리보기에 표시되고, 적은 치명타가 없다.
- **시선과 기습**: 순찰병은 사방을 살피고, 방심한 적은 **바라보는 쪽 2칸**만 본다(가끔 뒤를 돌아본다). 적에 마우스를 올리면 살피는 칸이 노랗게 보인다. 들키기 전에 치면 기습 +2.
- **발견 카드**: 위험 보물고의 금고와 봉인 상자 판정 성공 시 임시 카드 3장 중 1장을 그 원정 동안만 덱에 넣는다. 마음에 들면 귀환 후 연구해 영구화한다.
- 수호자는 예고 공격 외에, 다가와 곁에 닿으면 짧게 후려치고 4칸 넘게 달아나면 격노해 더 빨리 쫓는다.
- 수호자를 쓰러뜨리고 귀환하면 **추가 조건 없이** 다음 지역이 열린다. 전투를 피하려면 의뢰 게시판의 우회 항로(이전 지역 재료만 요구)를 복구한다.

## 현재 범위

미궁 3개(7·9·11방), 대원 3명(방어/반격 · 이동/밀치기/지형 · 상태이상/연쇄), 카드 34종(주사위 카드·판정 보조 포함), 적 24종(기본 14 + 특성 조립 10), 행동 유형 9종(근접·무리·사수·술사·중장·도약·방패병·투척병·수호자), 적 특성 9종, 수제 방 7개, 던전 기믹 3종(폭발통·밀 상자·레버), 지도 자동 이동·귀환 균열·귀환석, 지역 소진·재생, 배치식 마을 꾸미기(시설 이동·장식 11종·인접 보너스), 시설 4종×3단계, 장비 10종, 의뢰 7개, 행상(금화로 재료 구입), 자동 저장·백업·내보내기/가져오기·손상 감지·탭 충돌 감지.

## 테스터 안내

- **플레이 주소**: https://doridori0150.github.io/emberwake-reborn/ (브라우저 저장. 기기를 옮길 때는 저장 관리 → 내보내기/가져오기)
- **첫 세션 과제(30~60분)**: 새 게임 → 첫 원정에서 재료를 모아 귀환 → 제작 공방 복구 → 두 번째 원정에서 전투 한 번 이상 → 세 번째 원정 안에 뿌리 회랑 수호자 도전. "어디서 막혔는지, 무엇을 모르겠는지"가 가장 값진 제보다.
- **버그 제보**: GitHub Issues https://github.com/doridori0150/emberwake-reborn/issues (또는 개발자에게 직접). 제목에 상황 한 줄, 본문에 **기대한 것 / 실제 일어난 것 / 재현 순서**, 그리고 설정 → **진단 정보 복사**로 나온 글(버전·커밋·콘텐츠 해시·원정 시드·최근 행동 40개·최근 오류·저장 전체)을 붙여 넣는다. 복사 전에 포함 항목을 확인창으로 알려 주며, 복사가 막힌 환경에서는 글상자가 뜬다.
  - 예: "수문장 방에서 귀환 균열을 눌러도 아무 일이 없음. 기대: 귀환 확인 창. 실제: 안내줄만 깜빡임. 순서: 수문장 처치 → 균열 옆에서 E."
- **후반부터 보기**: 저장소의 `saves/late-30.json`(캠페인 봇이 30원정 진행한 저장)을 저장 관리 → 파일에서 가져오기. 가져오기 전 현재 진행은 자동 백업되고, 저장 관리의 백업 목록에서 언제든 되돌릴 수 있다.
- **지난 원정 기록**: 정산 뒤 설정 → 지난 원정 기록.
- 알려진 문제: 3지역(별의 서고) 성소 도달이 시간에 빠듯하다 · 노아가 후반에 약하다 · 행상 가격은 미조정 · 기믹·불길·2·3지역 적 그림은 임시.

## 콘텐츠 도구 (editor.html)

**`에디터 열기.bat` 을 더블클릭**하면 서버가 뜨고 브라우저에 도구가 열린다(`게임 실행.bat` 은 게임). 검은 창을 닫으면 서버가 꺼진다. 직접 하려면 `npm start` 후 `http://127.0.0.1:8812/editor.html`. 각 탭 위의 "사용법"에 단계별 안내가 있고, Ctrl+Z 되돌리기·Ctrl+S 저장·도구 단축키(V/B/P/H/N/C/D/E/R/X)·끌어 옮기기를 쓴다. 편집 대상은 `src/content.js` 하나이고, 저장하면 로컬 서버가 그 파일을 다시 쓴다(직전 파일은 `dist/content.prev.js`). 서버 없이 열면 읽기 전용이며 "텍스트로 보기"로 본문을 복사할 수 있다.

- **방 검수**: 지역·방 종류·시드를 골라 실제 생성기로 방 12~48개를 한 화면에 그린다. 경비 관계(붉은 선)·순찰로(노란 점선)·경고를 보여 주고, 마음에 드는 방은 "에디터로 복사".
- **방 에디터**: 13×9 격자에 기둥·위험 지형·소품·적을 찍고 경비/순찰/시선을 지정한다. 검사(문 앞 비움, 닿을 수 없는 곳, 봉인 장치 수, 재료 공급량, 경비 반경)는 생성기와 같은 함수(`map.lintRoom`)다. "시험 플레이"는 저장 없이 그 방에서 바로 시작한다(게임 저장을 건드리지 않는다).
- **제작·장비**: 무기(기본 공격 주사위 `1d8+1` 등, 범위·평균 즉시 표시)·가방(칸 수·묶음 크기)·장신구의 제작 목록, 재료 비용, 공방 단계, 효과, 전용 대원을 편집한다. **제작 옵션**(날 세우기, 광석 주머니…)을 만들고 장비마다 붙일 수 있는 옵션과 옵션 칸 수를 정한다.
- **이벤트**: 트리거(원정 시작·방 첫 입장·수호자 처치·귀환·시설 복구·길드 방문) + 조건(지역·방·플래그·확률) → 초상화가 있는 대화 → 선택지(조건·효과·답하는 말·이어질 이벤트). 초상화 그림 올리기, 대화창 미리보기.
- **마을**: 로비를 걸어 다니며 만나는 주민(이름·모습·색 필터·자리(지도 클릭)·맡은 시설·인사말·등장 조건)을 편집하고, 주민별 대화 이벤트를 바로 만든다.
- **기본 수치**: 가방 기본 칸, 재료 묶음·판매가, 대원 체력·이동·맨손 주사위, 치명타·투지, 방 구성 규칙. 바꾼 값만 덮어쓰기로 저장되고 항목별로 기본값 복귀.
- **몬스터 작업대**: 수치·그림·색 필터·행동 유형·전리품·특성을 폼으로 고치고, 옆에서 세 대원과 100판 모의전을 돌려 비교 대상 대비 받은 피해를 본다. 어느 지역·방에 어떤 조합으로 나올지도 여기서 넣는다. 기본 적을 고치면 덮어쓰기로 저장되고 "기본값으로 되돌리기"가 된다.

새 **특성**(조립식 부품)을 만들 때만 코드를 고친다: `data.js`의 `TRAITS`에 이름·매개변수·설명을, `run.js`의 같은 id 위치(장갑 `armorOf`, 처치 `killEnemy`, 타격 `strike`, 차례 시작 `actEnemy`)에 규칙을 넣으면 도구의 폼과 툴팁에 자동으로 나타난다. 방 구성 수치(경비 반경, 수제 방 확률 등)는 `RULES.level`.

## 구조

```
index.html · styles.css
src/rng.js          분리된 난수 흐름(map/loot/draw/dice/ai)과 주사위 판정
src/content.js      도구가 관리하는 콘텐츠(JSON): 추가 적·등장 조합·수제 방. data.js 가 합친다
src/data.js         콘텐츠 표: 재료·카드·대원·적·특성·지역·시설·장비·연구·의뢰
src/events.js       이벤트 공용 규칙: 트리거·조건 맞추기, 선택지 조건, 효과 글, 검사
src/mapgen.js       방 그래프·타일, 구성 배치(목표 지점·경비·순찰·지형), 수제 방 적용·검사, 도달 가능성 검증
src/run.js          원정 규칙 엔진(순수 상태 전이, DOM 없음). 미리보기와 실제 피해가 같은 계산을 쓴다
src/guild.js        길드 경제·투자·덱/장비·정산(원정 ID당 1회)
src/save.js         버전 있는 IndexedDB 저장, 체크섬, 백업, 탭 충돌 방지
src/render.js       에셋 로더 + 원정 캔버스 렌더러(이벤트 재생, 그림과 판정 분리)
src/guildview.js    마을 캔버스(카메라가 따라오는 걸어 다니는 로비, 주민·행인·낮/해 질 녘, 복구 단계에 따라 외형 변화)
src/app.js          화면 전환·입력·HUD·패널·모달
src/dialog.js       대화창(초상화·페이지·선택지). 게임과 도구 미리보기가 함께 쓴다
src/audio.js        합성 효과음
src/bot.js          대원별 전투 봇(모의전). 도구와 tools/smart-bot.cjs 가 함께 쓴다
editor.html · src/editor.js · src/editor2.js · src/contentfmt.js   콘텐츠 도구와 content.js 포맷터
tools/              server · build · import-assets · town-assets · process-generated.py · sim · boss-duel  (코드 모양은 `npx prettier --write "src/**/*.js" "tools/**/*.cjs" "tests/**/*.cjs"`)
tests/core.test.cjs 자동 검사
docs/DESIGN.md      진단·규칙·경제·세 빌드  /  docs/WORKLOG.md  개선 주기·직접 플레이 기록·남은 문제
```

## 에셋과 교체 방법

그림은 모두 참조 프로젝트의 실행용 WebP를 **복사**한 것이다(원 출처: 참조 프로젝트 `CREDITS.md`·`ASSET-PIPELINE.md` — 내장 imagegen으로 만든 독자 디자인). 이번 작업에서 **새로 생성한 이미지는 없다.**

- 코드와 저장은 파일 경로가 아니라 의미 기반 ID(`actor.ara`, `enemy.warden`, `tiles.labyrinth`, `material.ore`, `facility.workshop.1`, `cardart.fire` …)만 쓴다.
- 교체: `assets/`의 파일을 바꾸거나 `tools/import-assets.cjs`의 매핑을 고친 뒤 `npm run assets` → `npm run build`. 캐릭터는 프레임별 `[x, y, w, h, pivotX, pivotY]`와 `bodyHeight`(발 기준점·몸 높이)로 배치되므로 원본 크기가 달라도 타일 크기는 변하지 않는다.
- 임시/부족 에셋 목록은 [docs/WORKLOG.md](docs/WORKLOG.md)의 "에셋 현황"에 있다.
